dockerized foundry-next starter.

## getting started

build the contracts and export the type bindings:
```bash
build:contract
```
run the development server:

```bash
yarn dev
```

open [http://localhost:3000](http://localhost:3000) with your browser to see the result.